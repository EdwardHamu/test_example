# 会话完成提示音调整

- 修改 `assets/arena-model-probe.inject.js` 的通知音频逻辑。
- 自定义提示音时长从 0.45 秒延长为 1.50 秒，峰值增益从 0.12 调至 0.20（不代表感知音量同比增加）。保留 C5/E5/G5 三音，加入短渐入、持续段与渐出。
- 启用通知时统一播放自定义声音；桌面通知使用 silent:true，避免系统默认音与自定义音叠加。通知关闭时不播放。
- AudioContext 挂起时尝试 resume；播放结束或异常时释放上下文。不等待声音结束再触发 Esc，不改变下一轮抽卡 10 秒冷却。
- 浏览器自动播放策略、页面静音和系统音量仍可能阻止或影响声音；未在真实桌面环境试听。建议刷新后先点击页面，再通过测试通知验证。
- 新增 `tests/completion-chime.test.cjs`，验证长度、增益、权限分支去重、通知开关、resume 与清理。共 62 项测试通过；JS 语法检查与 git diff --check 通过。
- 未修改 EXE，未提交或推送 Git。
