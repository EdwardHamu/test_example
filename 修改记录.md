# Arena 筛选助手与探测脚本修改记录

本文档详细记录了针对本项目（ArenaModelCompanion）所做的功能增强与参数调整，方便后续维护、升级或复原。

---

## 1. 会话结束自动弹出系统通知

- **修改文件**：[`assets/arena-model-probe.inject.js`](file:///E:/Code/ArenaModelCompanion/assets/arena-model-probe.inject.js)
- **需求目标**：当 Arena 会话/对话生成完毕时，自动向 Windows 系统发送原生通知提醒用户。

### 实现细节
1. **版本升级**：
   - 脚本版本由 `1.0.0+cf897f9f` 升级为 `1.1.0+cf897f9f`，确保在已加载旧版脚本的页面中能识别版本变化并重新挂载。
2. **结束帧快速判定**（`interceptor.js` / `consumeFrame`）：
   - 在流帧解析中增加了对结束帧（`tag === 'finish'`、`obj.finishReason`、`obj.stop_reason`、OpenAI `choices.finish_reason`、Google `candidates.finishReason`）的检测。
   - 收到结束帧后立即调用 `this.publish(true)`，提前将观测状态置为 `complete: true`，不依赖连接断开。
3. **新增系统通知模块**（`__mods["notifier"]`）：
   - **原生系统级横幅**：基于标准 W3C HTML5 `Notification` API，弹出 Windows 10/11 操作中心 Toast 通知并播放系统提示音。
   - **点击唤醒窗口**：点击通知横幅自动调用 `window.focus()` 并关闭当前通知。
   - **双通道结束侦测引擎**（`initSessionWatcher`）：
     - 结合网络流完成事件（`obs.complete`）与页面 DOM 状态（监听「`Stop generating` / 停止生成」按钮消失）。
     - 配备 500ms 防抖计时器，防止多步骤/工具调用过程中的短暂空档误报。
     - 具备轮次去重锁（`lastNotifiedGeneration`），避免单轮对话重复弹窗。
   - **多槽位与模型信息提取**：
     - 单模型：显示模型真实名称（如 `GPT-6 系列 (gpt-6-astra-high)`）、生成耗时与 Token 数统计（含思考链 Token）。
     - 双模型对战：显示 `模型 A: ... · 模型 B: ...` 以及总耗时。
   - **降级保护**：
     - 若系统通知权限未被允许，自动降级为 Web Audio API 柔和三音阶提示（C5-E5-G5）以及网页标题闪烁（`【回答完成】...`）。
   - **状态持久化**：
     - 通知开关默认开启，并在 `localStorage['amp_session_notify_enabled']` 中记忆用户选择。
4. **HUD 面板与 API 支持**：
   - 在悬浮面板操作区添加「`通知: 开启/关闭`」开关与「`测试通知`」按钮。
   - 在 `window.__MODEL_PROBE__` 上暴露全局方法：
     - `notify(title, body, meta)`
     - `testNotification()`
     - `setNotificationEnabled(val)`
     - `isNotificationEnabled()`

---

## 2. 模拟抽卡每轮完成间隔 15 秒

- **修改文件**：[`Arena筛选助手.exe`](file:///E:/Code/ArenaModelCompanion/Arena筛选助手.exe)
- **备份文件**：[`Arena筛选助手.exe.bak`](file:///E:/Code/ArenaModelCompanion/Arena筛选助手.exe.bak)
- **需求目标**：模拟抽卡每完成一次对话改名后，等待 15 秒再自动进入下一轮。

### 实现细节
1. **原版逻辑定位**：
   - 抽卡主循环位于程序集内 `ArenaCompanion.MainForm+<RunGachaLoop>d__bc.MoveNext`。
   - 原版在每轮改名完成并写入日志（`HH:mm:ss 抽卡完成 X/Y`）后，通过 `await Task.Delay(400)`（文件偏移量 `0x0000E141`）仅暂停 400 毫秒（0.4 秒）。
2. **字节码精准修改**：
   - 原 IL 指令：`20 90 01 00 00`（`ldc.i4 400`）
   - 修改后指令：`20 98 3A 00 00`（`ldc.i4 15000` = 15,000 毫秒 = 15 秒）
   - 经 .NET 反射验证，修改后每轮完成后等待间隔精准固定为 15 秒。
3. **备份与生效说明**：
   - 原版未修改的程序已自动备份保存为 `Arena筛选助手.exe.bak`。
   - **生效方式**：完全退出当前正在运行的 `Arena筛选助手` 进程，重新双击打开 `Arena筛选助手.exe` 即可生效。
   - **还原方式**：如需恢复原版 400ms 间隔，只需将 `Arena筛选助手.exe.bak` 覆盖回 `Arena筛选助手.exe` 即可。

---

## 3. 会话结束自动触发 Esc 键

- **修改文件**：[`assets/arena-model-probe.inject.js`](file:///E:/Code/ArenaModelCompanion/assets/arena-model-probe.inject.js)
- **需求目标**：当 Arena 会话/对话生成完毕时，自动模拟触发键盘 `Esc`（Escape）键，以便及时关闭临时浮层/弹窗、退出输入框焦点或响应页面的快捷取消动作。

### 实现细节
1. **版本升级**：
   - 脚本版本由 `1.1.0+cf897f9f` 升级为 `1.2.0+cf897f9f`，便于已挂载旧版脚本的页面在下一次文档创建或探测器启动时自动识别并重载挂载。
2. **键盘事件精准仿真与广播**（`triggerEscapeKey`）：
   - **全规格事件构造**：严格遵循标准 W3C DOM Level 3 规范构造 `KeyboardEvent`，精准配置 `key: 'Escape'`、`code: 'Escape'`、`keyCode: 27`、`which: 27`，并赋予 `bubbles: true`、`cancelable: true`、`composed: true`（支持穿透 Shadow DOM 边界）。
   - **多层次派发保证**：
     - 首先派发至当前获得焦点的目标元素（`document.activeElement`，如输入框、操作按钮或浮层内部元素），事件沿 DOM 树自然冒泡。
     - 若当前焦点为非顶层元素（如处于具体组件内），额外向 `document` 与 `window` 全局广播派发，确保基于全局绑定的弹窗库（如 Radix UI、Headless UI、React Modal 等）以及页面快捷键监听器均能稳定捕获 `Escape` 事件。
   - **多层降级容错**：若当前运行宿主未完全实现 `KeyboardEvent` 构造器，自动向下回退至 `document.createEvent('KeyboardEvent')` 或标准 `Event`，确保任何极端环境下均不抛出异常。
3. **会话结束联动机制**：
   - 与 `initSessionWatcher` 的 `onSessionEnd` 结束侦测引擎深度整合。在流结束帧（finish 帧）与页面 DOM 停止生成状态（500ms 防抖确认）判定会话结束时，与桌面系统通知协同联动触发。
4. **状态持久化与 HUD 控制**：
   - **状态记忆**：自动 Esc 功能默认开启，并在 `localStorage['amp_session_auto_esc_enabled']` 中持久化记录用户选择。
   - **HUD 操作控制**：在悬浮面板操作按钮区新增「`Esc: 开启/关闭`」切换按钮（支持激活高亮）以及「`测试 Esc`」模拟测试按钮。
   - **实时日志反馈**：触发后在 HUD 终端日志中实时打印 `🔔 会话结束：...（已自动触发 Esc 键）`，便于排查追踪。
5. **全局 API 暴露**：
   - 在 `window.__MODEL_PROBE__` 全局对象上公开对应接口，方便用户或自动化测试在控制台调用：
     - `triggerEsc()`：立即手动模拟触发一次 Esc 按键。
     - `setAutoEscEnabled(val)`：设置会话结束自动触发 Esc 键开关。
     - `isAutoEscEnabled()`：查询当前是否开启会话结束自动触发 Esc 键。

---

## 4. 文件变更汇总

| 文件路径 | 变更类型 | 说明 |
| :--- | :--- | :--- |
| [`assets/arena-model-probe.inject.js`](file:///E:/Code/ArenaModelCompanion/assets/arena-model-probe.inject.js) | 代码修改 | 升级至 v1.2.0，增加会话结束通知模块、结束帧判定与会话结束自动触发 Esc 键功能 |
| [`Arena筛选助手.exe`](file:///E:/Code/ArenaModelCompanion/Arena筛选助手.exe) | 二进制修改 | 将 `RunGachaLoop` 中的轮次延迟从 400ms 改为 15,000ms（15秒） |
| [`Arena筛选助手.exe.bak`](file:///E:/Code/ArenaModelCompanion/Arena筛选助手.exe.bak) | 新增文件 | 原版可执行文件完整备份 |
| [`修改记录.md`](file:///E:/Code/ArenaModelCompanion/修改记录.md) | 文档更新 | 详细记录通知弹窗、抽卡 15 秒间隔以及自动触发 Esc 键的实现细节与参数配置 |
